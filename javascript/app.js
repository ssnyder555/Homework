console.log('working');


//What is the difference between a parameter and an argument?

// A parameter is a variable in a method definition.
// When a method is called, the arguments are the data you
 // pass into the method's parameters. Parameter is variable
 // in the declaration of function. Argument is the actual value of
 // this variable that gets passed to function.


// Within a function, what is the difference between
// return and console.log?

// 1 will be stored in the variable a. Return is
// the result a subroutine, or function, will return,
//and console.log is a way to log to the "console" where
// available. The console is a destination made available
// by certain browsers that enables Javascript programmers
// to stream text to an output e.g logging.

// What are the implications of the ability of
// a function to return a value?


// 12.4 Functions that Return Values.
// A return is a value that a function returns to the
// calling script or function when it completes its
// task. A return value can be any one of the four variable
// types: handle, integer, object, or string. The type of value
// your function returns depends largely on the task it performs.

 // 2. Write a function checkPalindrome that accepts a
// single argument, a string. Yes, you've done it before,
// but do it again. Later in this assignment we're gonna beef up
// our palindrome function some. See if you can do it without looking
// back at your previous answer. The function should return true if
// the string is a palindrome, false if not. Make sure your function
// will give the correct answer for words with capital letters.




// const checkPalidrome = (word) => {
   // console.log(word.split(" "));
// }


// console.log(checkPalindrome("Radar"));
 // => true
// console.log(checkPalindrome("Borscht"));
// => false

// 3. Write a function sumDigits that accepts a number
// and returns the sum of its digits.

const sumDigits = (i ==> 0){
  
}

console.log(sumDigits(42));
=> 6;
